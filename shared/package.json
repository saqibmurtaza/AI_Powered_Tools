{
  "name": "shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/**/*.ts --format=esm --dts --external=react",
    "dev": "tsup src/**/*.ts --format=esm --dts --external=react --watch"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "tsup": "^8.3.5",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "tailwind-merge": "^2.6.0"
  }
}